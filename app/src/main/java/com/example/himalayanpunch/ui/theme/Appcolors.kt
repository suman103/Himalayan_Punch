package com.example.himalayanpunch.ui.theme

import androidx.compose.ui.graphics.Color

// ── HimalayanPunch Design System ──────────────────────────────────────────────
// Warm coffee-house aesthetic: deep espresso + warm cream, soft textures, cozy type

object CafeColors {
    // Warm Dark Backgrounds
    val Espresso    = Color(0xFF1A0F0A)   // deepest bg
    val Roast       = Color(0xFF2A1A10)   // surface 1
    val Mocha       = Color(0xFF3A2518)   // surface 2
    val Latte       = Color(0xFF4A3225)   // surface 3

    // Primary Accent — Warm Amber / Coffee Gold
    val Amber       = Color(0xFFD4841A)
    val AmberLight  = Color(0xFFEDA73A)
    val AmberDim    = Color(0xFF3D2506)

    // Secondary — Sage Green (tea vibes)
    val Sage        = Color(0xFF6B8F5E)
    val SageDim     = Color(0xFF1E2D1A)

    // Cream / Milk tones
    val Cream       = Color(0xFFFFF8F0)
    val CreamWarm   = Color(0xFFF5E6D0)
    val CreamDim    = Color(0xFF2D1F14)

    // Text
    val TextPrimary   = Color(0xFFFFF8F0)   // warm white
    val TextSecondary = Color(0xFFBFA898)
    val TextMuted     = Color(0xFF7A6258)

    // Status
    val Green       = Color(0xFF6B8F5E)
    val GreenDim    = Color(0xFF1A2D16)
    val Red         = Color(0xFFB94040)
    val RedDim      = Color(0xFF2D1010)
    val Yellow      = Color(0xFFD4841A)

    // Category
    val HotColor    = Color(0xFFD4841A)
    val ColdColor   = Color(0xFF5B8FA8)
    val FoodColor   = Color(0xFF8F6B3E)

    // Divider
    val Divider     = Color(0xFF3A2518)
    val DividerLight= Color(0xFF4A3225)
}